export interface SlideOptions {
    initialSlide?: number,
    direction?: string,
    speed?: number,
    effect?: string,
    spaceBetween?: number,
    slidesPerView?: number,
    freeMode?: boolean,
    loop?: boolean
};